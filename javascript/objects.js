// Declaração de um objeto, semelhante aos arrays.

const userName = {
    first: 'Rian ',
    middle: 'Dias ',
    last: 'de Oliveira'
};

console.log(userName.first + userName.middle + userName.last);