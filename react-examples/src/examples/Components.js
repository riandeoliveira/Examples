// Componentes.

// Partes independentes de uma aplicação que podem ser reutilizadas, inclusive entre si, e renderizadas. Sua sintaxe é de uma função ou uma classe, que aceitam props como entrada e retornam elementos React em JSX. 

const Components = () => {
    return (
        <>
            <h2>Componente</h2>
            <p>Este é um exemplo de componente.</p>
        </>
    );
}
 
export default Components;